# Core dependencies for SDG Ethiopia Chatbot
# Optimized for Streamlit Cloud deployment (no vector DB)
# Install with: pip install -r requirements.txt

# Data processing
pandas
numpy

# LLM and orchestration - Using NEW google.genai package (version that supports AQ. keys)
google-genai==2.10.0
python-dotenv

# Web interface
streamlit

# Utilities
requests

# Telegram bot (optional)
python-telegram-bot

# Web scraping for live ESS data
beautifulsoup4
lxml

# Image processing for logo display
Pillow
